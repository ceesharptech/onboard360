// Seed script stub
// This will be filled in during Phase 2 with default onboarding templates per common role/department.

async function main() {
  // To be implemented in Phase 2
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
